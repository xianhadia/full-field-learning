# full-field-learning
Using neural operators (e.g. DeepONet) to learn full field data to improve surrogate modeling.


Trained on single l40 GPU.

Tested on CPU Xeon (list hardware)

Versions:


Exact commands to recreate training and testing shown in sbatch scripts in related folders.

